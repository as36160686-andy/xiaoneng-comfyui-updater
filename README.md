# 小能ComfyUI启动器 · 自动更新托管

本仓库由 `setup_github.py` 自动维护，存放 `latest.json` 与增量补丁。
更新地址（base_url）：`https://as36160686-andy.github.io/xiaoneng-comfyui-updater`
请勿手动修改 `latest.json` / `patches/`，改由 `make_release.py` 重新生成后推送。
